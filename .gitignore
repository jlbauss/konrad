# Model-as-working-memory files (see image/opencode/AGENTS.md
# "Planning workflow"). The agent writes these into /workspace; if the
# konrad repo is itself the workspace (dev mode), keep them out of git.
task_plan.md
progress.md
findings.md

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log
